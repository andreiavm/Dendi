import React from 'react'
import styles from '../../styles/product.module.css'

const ProductDetails = () => {
    return (
        <div className={`${styles.product_page} container`}>Product</div>
    )
}

export default ProductDetails;